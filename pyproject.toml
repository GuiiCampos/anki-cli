[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "anki-cli"
version = "0.1.0"
requires-python = ">=3.8"
dependencies = ["requests"]

[project.scripts]
anki-cli = "anki_cli.main:main"

[tool.setuptools.packages.find]
where = ["src"]